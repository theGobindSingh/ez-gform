# Getting Started

There is no docs/playground site yet (`@ez-gform/docs` has not been built).
Until then, the best place to start is `packages/core/README.md`, which
covers installing `@ez-gform/core` and its parser/encoder/submit API
directly.
