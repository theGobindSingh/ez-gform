---
"@ez-gform/core": minor
---

Initial release of `@ez-gform/core`: parses `FB_PUBLIC_LOAD_DATA_` into a typed `FormSchema`, encodes `FormValues` into Google Forms' `entry.NNN` wire format (text, checkboxes, "Other", date/time, grids), builds prefill URLs and submit bodies, and submits via `fetch`.
