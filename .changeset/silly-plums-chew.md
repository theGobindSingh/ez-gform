---
"@ez-gform/react": minor
---

Initial release of `@ez-gform/react`: `useGoogleForm`, a controlled-values hook with an explicit `idle`/`validating`/`submitting`/`sent`/`ok`/`error` status machine, promise-returning `submit`, schema-driven validation, and `register`/`registerCheckbox` field bindings.
